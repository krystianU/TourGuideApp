<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.tourguideapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RestaurantsActivity"
            android:label="@string/activity_restaurants"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".MuseumsActivity"
            android:label="@string/activity_museums"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".MonumentsActivity"
            android:label="@string/activity_monuments"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".NightLifeActivity"
            android:label="@string/activity_night_live"
            android:parentActivityName=".MainActivity">

        </activity>

    </application>

</manifest>